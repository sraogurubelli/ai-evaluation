apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ai-evolution-data
  namespace: ai-evolution
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
  # Note: Update storageClassName based on your cluster's storage classes
