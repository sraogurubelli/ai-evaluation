# Adapter examples

`example_custom_adapter.py` — template for implementing the Adapter interface (auth, API calls, response parsing). Copy and modify for your API. For most REST APIs use `HTTPAdapter`; see [docs/custom-adapters.md](../../docs/custom-adapters.md).
