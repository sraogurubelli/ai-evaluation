# Adapters

Connect to AI systems via the `Adapter` interface. Use `HTTPAdapter` for REST APIs (see [docs/custom-adapters.md](../../docs/custom-adapters.md)). Create custom adapters in your repo; see `examples/adapters/` and `samples_sdk/consumers/devops`.
